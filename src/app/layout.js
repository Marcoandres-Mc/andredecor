import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Andre Decor | Forro de muebles y cortinas en Lima",
  description: "Servicio profesional de forrado de muebles, cortinas, rollers y lavado en Lima. Instalamos en tu hogar.",
  keywords: ["forro de muebles", "cortinas Lima", "rollers", "lavado de muebles"],
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
